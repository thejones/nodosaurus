import {Polygon} from './../../../global/controllers';

export default class Neighborhoods extends Polygon {
  constructor(options = {}) {
    super();
    console.log(' geo constructor called');
  }
}
